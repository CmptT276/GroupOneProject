<h1>All Products</h1>

<ul>
  <% @products.each do |p| %>
  <li>
      <h3><%= p.name %></h3>
      <small><%= number_to_currency(p.price) %></small>
      <p><%= p.description %></p>
  </li>
  <% end %>
</ul>